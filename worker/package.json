{
  "name": "streamsense-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Kafka metrics aggregation worker for StreamSense",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "kafkajs": "^2.2.4",
    "pg": "^8.13.1"
  }
}
