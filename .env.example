# The Docker stack works with defaults. These values document the local demo.
POSTGRES_DB=streamsense
POSTGRES_USER=streamsense
POSTGRES_PASSWORD=streamsense
KAFKA_TOPIC=events
EVENT_INTERVAL_MS=350

# Frontend: "demo" uses reproducible browser data; "live" calls /api/kpis.
VITE_DATA_MODE=demo
